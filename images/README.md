# Images

Put your header banner image here as `header.jpg` (this is the name
`config.js` looks for).

It displays at roughly 800×225 px (a wide banner), so a landscape image
works best. To turn the banner off entirely, set `headerImage.enabled`
to `false` in `../config.js`.
